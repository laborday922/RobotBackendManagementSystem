<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.data.clean.mapper.CleanRuleMapper">

    <select id="selectById" resultType="com.ruoyi.data.clean.mapper.po.CleanRulePo">
        SELECT id,
               execute_mode,
               apply_data_source,
               config_json,
               cron_expression,
               tenant_id
        FROM clean_rule_config
        WHERE id = #{id}
        <if test="tenantId != null">
            AND tenant_id = #{tenantId}
        </if>
    </select>

    <select id="getTableColumns" resultType="String">
        SELECT column_name
        FROM information_schema.columns
        WHERE table_name = #{tableName}
          AND column_name != 'id'
    </select>

    <select id="selectScheduledRules" resultType="com.ruoyi.data.clean.mapper.po.CleanRulePo">
        SELECT id,
               execute_mode,
               apply_data_source,
               config_json,
               cron_expression,
               tenant_id
        FROM clean_rule_config
        WHERE execute_mode = 'SCHEDULED'
        <if test="tenantId != null">
            AND tenant_id = #{tenantId}
        </if>
    </select>

    <insert id="batchInsertResults">
        INSERT INTO clean_interaction_result
        (source_id, task_id, config_id, raw_content, clean_content, status_label, clean_time, tenant_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.sourceId},
            #{item.taskId},
            #{item.configId},
            #{item.rawContent},
            #{item.cleanContent},
            #{item.statusLabel},
            #{item.cleanTime},
            #{item.tenantId}
            )
        </foreach>
    </insert>

    <select id="selectRawInteractionData" resultType="map">
        SELECT id, evaluation_text
        FROM t_interaction_history
        WHERE evaluation_text IS NOT NULL
        AND evaluation_text != ''
    </select>

</mapper>
