<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mode.mapper.SysRobotMapper">

    <!-- 保存机器人模式配置（JSON格式）。tenant_id 由调用方传入，不再依赖 sys_robot_ext -->
    <insert id="saveRobotModeConfig">
        INSERT INTO sys_robot_mode_config (robot_id, mode_id, config, tenant_id, create_time, update_time)
        VALUES (#{robotId}, #{modeId}, #{config}, #{tenantId}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE
        config = #{config},
        update_time = NOW()
    </insert>

    <!-- 获取机器人模式配置 -->
    <select id="getRobotModeConfig" resultType="String">
        SELECT config
        FROM sys_robot_mode_config
        WHERE robot_id = #{robotId}
        AND mode_id = #{modeId}
        LIMIT 1
    </select>

    <!-- 删除机器人模式配置 -->
    <delete id="deleteRobotModeConfig">
        DELETE FROM sys_robot_mode_config
        WHERE robot_id = #{robotId}
        AND mode_id = #{modeId}
    </delete>

</mapper>
